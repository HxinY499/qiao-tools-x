# 2026-03-09

## 为 pages 下的 utils 文件补充单测

- 安装了 `vitest` 作为测试框架
- 在 `package.json` 添加了 `test` 和 `test:watch` 脚本
- 为以下 7 个工具页面的 `utils.ts` 编写了单测（共 145 个测试用例）：
  1. `json-formatter/utils.test.ts` - 9 tests（JSON 解析、错误信息）
  2. `json-schema-converter/utils.test.ts` - 33 tests（Schema↔TS 双向转换、验证）
  3. `text-escaper/utils.test.ts` - 18 tests（HTML/Unicode/JS 转义互逆）
  4. `text-diff/utils.test.ts` - 14 tests（diff 行构建、统计、摘要）
  5. `regex-visualizer/utils.test.ts` - 31 tests（flags、正则创建、匹配、替换、解释、高亮）
  6. `sse-to-json/utils.test.ts` - 18 tests（SSE 解析、信号识别、字段提取）
  7. `qrcode-tool/utils.test.ts` - 22 tests（WiFi/vCard/SMS/Geo 格式化、坐标验证）

- 跳过了以下因依赖 DOM/Canvas/浏览器 API 不适合纯单测的文件：
  - `markdown-editor/utils.ts`（DOMParser, HTMLElement, document）
  - `seo-analyzer/utils.ts`（DOMParser）
  - `svg-converter/utils.ts`（DOM, Canvas）
  - `user-agent-parser/utils.ts`（只是 ua-parser-js 的简单 wrapper）
